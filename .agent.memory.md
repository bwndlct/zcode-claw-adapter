**必须遵守：如果本文件夹内容发生变化，务必评估并更新本文档。**

## 文件夹摘要 (Directory Summary)

> 描述本文件夹在工程中的位置、定位与作用。

本仓库是 Claw 与 ZCode 之间的本地 custom-engine 适配层源码库。Claw 负责编排，ZCode 通过 `app-server --stdio` 持有真实编码会话。

## 文件夹内容结构 (Structure)

> 描述主要文件/子目录的功能、关键类或函数。

- `adapter/bin/`：启动器与核心协议桥接。
- `adapter/lib/`：ZCode Protocol stdio 客户端和会话状态持久化。
- `scripts/install-local.mjs`：逐文件原子替换四个运行文件到 `~/.local/share/zcode-claw/adapter`。
- `package.json`：静态检查和本地安装入口。

## 遗留问题 (Legacy Issues)

> 记录暂时性妥协、技术债或未完成的优化。

- 尚无独立自动化协议测试；当前使用 Node 语法检查、安装一致性校验和显式 ZCode 任务回归。
- ZCode 桌面会话列表没有跨进程实时通知，adapter 通过后台 workspace deeplink 触发刷新。

## 特殊决策 (Special Decisions)

> 记录非标准实现的原因、用户指定的特殊要求（含来源）。

- 2026-09-02：按用户要求，源码放在本机 GitHub 工作区的独立 Git 仓库，运行时仍使用稳定的 `~/.local/share/zcode-claw/adapter` 安装路径。
- 2026-09-02：新 ZCode 会话创建后立即后台刷新桌面工作区，首次标题更新或首轮结束时最多再刷新一次。

## 注意事项 (Common Pitfalls & Conventions)

> 记录隐式约定、相似功能函数的区分与选择指南。

- 不要在仓库、日志、argv 或状态文件中写入 API key/token；启动器只通过子进程环境传递授权。
- 不要直接维护安装副本；先改本仓库，再执行 `npm run install:local`。
- 安装器只允许覆盖四个明确的 adapter 文件，不得删除 Claw 依赖、凭据或会话状态。
