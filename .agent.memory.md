**必须遵守：如果本文件夹内容发生变化，务必评估并更新本文档。**

## 文件夹摘要 (Directory Summary)

> 描述本文件夹在工程中的位置、定位与作用。

本仓库是 Claw 与 ZCode 之间的本地 custom-engine 适配层源码库。Claw 负责编排，ZCode 通过 `app-server --stdio` 持有真实编码会话。

## 文件夹内容结构 (Structure)

> 描述主要文件/子目录的功能、关键类或函数。

- `adapter/bin/`：启动器与核心协议桥接。
- `adapter/lib/`：ZCode Protocol stdio 客户端和会话状态持久化。
- `test/`：CLI、协议客户端、状态存储、npm 包安装和 fake app-server 集成测试。
- `scripts/install-local.mjs`：逐文件原子替换五个运行文件到 `~/.local/share/zcode-claw/adapter`。
- `package.json`：静态检查和本地安装入口。

## 遗留问题 (Legacy Issues)

> 记录暂时性妥协、技术债或未完成的优化。

- 真实 ZCode smoke 依赖本机凭据，不能进入公共 CI；CI 使用 fake app-server 覆盖协议回归。
- ZCode 桌面会话列表没有跨进程实时通知，adapter 通过后台 workspace deeplink 触发刷新。

## 特殊决策 (Special Decisions)

> 记录非标准实现的原因、用户指定的特殊要求（含来源）。

- 2026-09-02：按用户要求，源码放在本机 GitHub 工作区的独立 Git 仓库，运行时仍使用稳定的 `~/.local/share/zcode-claw/adapter` 安装路径。
- 2026-09-02：新 ZCode 会话创建后立即后台刷新桌面工作区，首次标题更新或首轮结束时最多再刷新一次。

## 注意事项 (Common Pitfalls & Conventions)

> 记录隐式约定、相似功能函数的区分与选择指南。

- 不要在仓库、日志、argv 或状态文件中写入 API key/token；启动器只通过子进程环境传递授权。
- 不要直接维护安装副本；先改本仓库，再执行 `npm run install:local`。
- 安装器只允许覆盖五个明确的 adapter 文件，不得删除 Claw 依赖、凭据或会话状态。

## 2026-09-02 公开化改造
- 新增公共 CLI 入口 `adapter/bin/zcode-claw-adapter.mjs`：--help/--version、ZCode 发现顺序（--zcode-bin > ZCODE_BIN > PATH > macOS bundle）、Desktop 凭据便利回退改为 Node 实现（不再依赖 jq）。npm 包名 `@bwndlct/zcode-claw-adapter`，MIT。
- `install:local` 现部署 5 个文件 + 最小 package.json（供运行副本 --version）。
- 旧 `zcode-engine-launcher.sh` 保留仅作本地兼容；公共入口统一走新 wrapper。

## 2026-09-03 发布后测试补全
- 自动化测试增加本地 tarball 离线全局安装、损坏/不支持的 Claw 输入，以及未知 ZCode 通知和 session event 的前向兼容覆盖。
- 真实 ZCode smoke 仍是凭据门控的发布验收，不进入公共 CI；公开 CI 只使用 fake app-server。
